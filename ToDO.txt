php/calendarFULL

-cadastrar: falta ser dinâmico a procurar o ccUtente ou a dar a possibilidade de registá-lo na hora
	falta ser dinâmico no Local e na intervenção, ou seja, os values vão ser printados automaticamente por cada tipoServico (nao sei se vale a pena perder tempo pq podemos adicionar uns quantos e uma opçao q seja Outro)

-editar: falta dar fix nos selects Local e Tipo intervençao que não estão a aparecer, mas acho q de resto está chill

-remover: Está certo

-visualizar: preço não poe decimais (p.e 132,33€ fica 12333)
	local e tipoServico aparecem as pks ids e não as descris  (option:selected)


<?php   
                                            
                                            $sql = "SELECT id, title FROM servico";
                                            $result = $conn->query($sql);

                                            if ($result->num_rows > 0) {
                                                // output data of each row
                                                while($row = $result->fetch_assoc()) {

                                                    $id = $row['id'];
                                                    $title = $row['title'];

                                                  echo "
                                                    <option value="$id" >"$title"</option>
                                                  ";


                                                }
                                            } 
                                        ?> 

<?php

                                        $sqlloc = "SELECT * from local";
                                        $resultloc = $conn->query($sqlloc); 

                                        echo '  <option selected hidden value=""></option>';


                                        if ($resultloc->num_rows > 0) {
                                            // output data of each row
                                            while($row = $resultloc->fetch_assoc()) {

                                                $codLocal = $row['codLocal'];

                                                $descriLocal = $row['descriLocal'];

                                                echo "
                                                    <option value=".$codLocal." >' .$descriLocal.' </option>
                                                  ";



                                            }
                                        }

                                         ?>

*****PAT, 17/nov, 7:11pm**********


SECÇÃO DE NOTAS PESSOAIS/ETC
